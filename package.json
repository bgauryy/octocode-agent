{
  "name": "octocode-agent-monorepo",
  "version": "1.0.0",
  "description": "Octocode agent monorepo — the branded agent launcher, the Pi harness extension, and the Awareness coordination layer.",
  "author": "Guy Bary <bgauryy@octocodeai.com>",
  "homepage": "https://octocode.ai",
  "repository": "bgauryy/octocode-agent.git",
  "bugs": "https://github.com/bgauryy/octocode-agent/issues",
  "license": "MIT",
  "type": "module",
  "packageManager": "yarn@4.9.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "yarn workspaces foreach -Apt run build",
    "lint": "yarn workspaces foreach -Apt run lint",
    "lint:fix": "yarn workspaces foreach -Apt run lint:fix",
    "test": "yarn workspaces foreach -Apt run test",
    "test:quiet": "yarn workspaces foreach -Apt run test:quiet",
    "typecheck": "yarn workspaces foreach -Apt run typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "prettier": "^3.8.4",
    "tsx": "^4.23.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.61.1"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
